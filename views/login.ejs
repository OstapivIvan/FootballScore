<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="login.css">
    <title>Log in</title>
</head>
<body class="login">
    <div class="log-form">
        <form action="/login" method="post" id="login_form" enctype="multipart/form-data">
            <h2>Log in</h2>
            <div class="form-control">
                <input type="email" id="Email" placeholder="Email" name="Email" required>
                <small>Error Message</small>
            </div>
            <div class="form-control">
                <input type="password" id="Password" placeholder="Password" name="Password" pattern=".{8,}" required>
                <small>Error Message</small>
            </div>
            <small id="errorContainer" class="<%= error ? 'error' : '' %>"><%= error %></small>
            <button type="submit">Enter</button>
            <br> <br>
            <span>
                <a href="/signup">Sign up</a>
                <a href="/">Skip</a>
            </span>
        </form>
    </div>
    <script src="validationFunc.js"></script>
    <script src="loginVal.js"></script>
</body>
</html>
